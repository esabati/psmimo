function [] = problem12()
cvx_begin
    variable t
    variable u
    variable V0(N,N)
    variable V1(N,N)
    variable V2(N,N)
    
    maximize(t)
    
    subject to
        u >= (1/rho)*(trace(V0) + trace(V1) + trace(V2)) + Pc % (7a)
        conj(h(:,1))'*V1*h(:,1) >= tau(
        

end
